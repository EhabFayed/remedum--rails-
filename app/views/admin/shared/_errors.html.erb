<% if record.errors.any? %>
  <div class="errors" role="alert">
    <b>تعذّر الحفظ — <%= record.errors.count %> مشكلة:</b>
    <ul>
      <% record.errors.full_messages.each do |m| %><li><%= m %></li><% end %>
    </ul>
  </div>
<% end %>
